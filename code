def main():
    for i in range(9):
     paint_corner(GREEN)
     move()
    paint_corner(GREEN)
    turn_left()
    move()
    paint_corner(GREEN)
    turn_left()
    move()
    paint_corner(GREEN)
    for i in range(8):
     paint_corner(GREEN)
     move()
    paint_corner(GREEN)
    turn_right()
    move()
    paint_corner(GREEN)
    turn_right()
    for i in range(9):
     paint_corner(GREEN)
     move()
    paint_corner(GREEN)
    turn_left()
    move()
    for i in range(4):
        move()
    paint_corner(RED)
    turn_left()
    move()
    for i in range(8):
        paint_corner(RED)
        move()
    paint_corner(RED)
    turn_right()
    move()
    paint_corner(RED)
    turn_right()
    for i in range(9):
        paint_corner(RED)
        move()
    paint_corner(RED)
    turn_left()
    move()
    paint_corner(RED)
    turn_left()
    for i in range(9):
        paint_corner(RED)
        move()
    paint_corner(RED)
    turn_left()
    for i in range(3):
        move()
    turn_left()
    for i in range(3):
        move()
    paint_corner(BLUE)
    for i in range(3):
        move()
        paint_corner(BLUE)
    turn_right()
    for i in range(3):
        move()
        paint_corner(BLUE)
    turn_right()
    for i in range(3):
        move()
        paint_corner(BLUE)
    turn_right()
    for i in range(2):
        move()
        paint_corner(BLUE)
    turn_right()
    move()
    paint_corner(BLUE)
    move()
    turn_right()
    move()
    paint_corner(BLUE)
def turn_right():
    turn_left()
    turn_left()
    turn_left()







if __name__ == "__main__":
    run_karel_program()
